define(['knockout','jquery', 'text!./home.html'],
    function(ko, $,  template) {

        function viewModel(params) {
        }

        return { viewModel: viewModel, template: template };
    });